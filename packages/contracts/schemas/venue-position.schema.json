{
  "$id": "https://nbe-theta.local/schemas/venue-position.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "A per-outcome inventory row on a venue.\n\n``shares`` is the signed inventory: positive = long the outcome,\nnegative = short it (Polymarket doesn't currently support short but\nthe shape stays neutral).\n``cost_basis`` is total dollars in for the current inventory;\ncombined with the last mid it produces unrealized P&L for the\ndashboard.",
  "properties": {
    "account_id": {
      "minLength": 1,
      "title": "Account Id",
      "type": "string"
    },
    "condition_id": {
      "minLength": 1,
      "title": "Condition Id",
      "type": "string"
    },
    "cost_basis": {
      "default": "0",
      "format": "decimal",
      "pattern": "^-?\\d+(\\.\\d+)?$",
      "title": "Cost Basis",
      "type": "string"
    },
    "outcome_token_id": {
      "minLength": 1,
      "title": "Outcome Token Id",
      "type": "string"
    },
    "schema_version": {
      "default": "1.0.0",
      "description": "Schema version of this payload; consumers reject on mismatch.",
      "title": "Schema Version",
      "type": "string"
    },
    "shares": {
      "format": "decimal",
      "pattern": "^-?\\d+(\\.\\d+)?$",
      "title": "Shares",
      "type": "string"
    },
    "venue": {
      "const": "polymarket",
      "title": "Venue",
      "type": "string"
    }
  },
  "required": [
    "venue",
    "account_id",
    "condition_id",
    "outcome_token_id",
    "shares"
  ],
  "title": "VenuePosition",
  "type": "object"
}
